id: test-64-neg-and-partial
info:
  name: Negative - AND Condition Partial Match
  severity: info
http:
  - method: GET
    path:
      - "{{BaseURL}}/test/structured"
    matchers-condition: and
    matchers:
      - type: word
        words:
          - "Welcome"
      - type: status
        status:
          - 404
